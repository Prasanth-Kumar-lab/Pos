import 'package:flutter/material.dart';

class TextColors{
  static const majorTextColor = Colors.black;
  static const minorTextColor = Colors.black54;
  static const buttonTextColor = Colors.white;
  static const buttonBackgroundColor = Colors.blue;
  static const circularProgressIndicatorColor = Colors.white;
  static const forgetPassword =  Colors.blue;
}

class IconsColors{
  static const profileIcon = Colors.blueGrey;
}

class AnimateShimmerColors{
  static const shimmerColor = Colors.white;
}

class AppBarIcons{
  static const appBarBg = Colors.green;
}

class LottieAssets{
  static const noDataFound = 'assets/No Data found.json';
}

class ScrollBarTheme{
  static const scrollBarThumbColor = Colors.green;
  static const scrollBarTrackColor = Colors.grey;
}

class CurvedContainer{
  static const curvedContainer = Colors.green;
}

class FormTextColors{
  static const majorFormTextColor = Colors.black;
  static const dividerColor = Colors.black;
}

class ProductsCardColors{
  static const borderSideColor = Colors.grey;
  static const activeBorderSideColor = Colors.green;
  static const availableProductCardColor = Colors.white;
  static const UnavailableProductCardColor = Colors.grey;
  static const availableBorderColor = Colors.green;
  static const unavailableBorderColor = Colors.red;
  static const productStatus = Colors.white;
  static const productStatusShimmerColor = Colors.black;
  static const productDecrementBg = Colors.red;
  static const productIncrementBg = Colors.green;
}
class PrintConstants{
  static const spaceBetweenWidgets = SizedBox(height: 5,);
  static const itemsTextStyle = TextStyle(fontWeight: FontWeight.bold, fontSize: 14);
  static const mainDetailsTextStyle = TextStyle(fontSize: 15);
}